const getters = {
    site_title: state => state.user.site_title,
    site_logo: state => state.user.site_logo,
    name: state => state.user.name,
    user: state => state.user.user,
    headimg: state => state.user.headimg,
    show_notice: state => state.user.show_notice,
    role_id: state => state.user.role_id,
    addRoutes: state => state.user.addRoutes,
    menu: state => state.user.menu,
    actions: state => state.user.actions,
    currentMenu: state => state.app.currentMenu,
    aside_state: state => state.app.aside_state,
    device: state => state.app.device,
    sidebar: state => state.app.sidebar,
    isIframe: state => state.app.isIframe,
    colorScheme: state => state.app.colorScheme,
    tags: state => state.tags.tags,
    tagsView: state => state.setting.tagsView,
    formSize: state => state.setting.formSize,
    inputWidth: state => state.setting.inputWidth,
    labelWidth: state => state.setting.labelWidth,
    mainBackgroundColor: state => state.setting.mainBackgroundColor,
    asideBackgroundColor: state => state.setting.asideBackgroundColor,
    asideActiveTextColor: state => state.setting.asideActiveTextColor,
    asideTextColor: state => state.setting.asideTextColor,
    headerBackgroundColor: state => state.setting.headerBackgroundColor,
    headerActiveBackgroundColor: state => state.setting.headerActiveBackgroundColor,
  }
  export default getters
